# Django + PostgreSQL Project

This project is a starter Django application configured to use PostgreSQL as its database backend.

## Setup Instructions

1. Create and activate the virtual environment:
   ```powershell
   python -m venv venv
   .\venv\Scripts\activate
   ```
2. Install dependencies:
   ```powershell
   pip install django psycopg2-binary
   ```
3. Configure PostgreSQL settings in `myproject/settings.py`.
4. Run migrations:
   ```powershell
   python manage.py migrate
   ```
5. Start the development server:
   ```powershell
   python manage.py runserver
   ```

## Project Structure
- `myproject/` - Django project root
- `venv/` - Python virtual environment

## Next Steps
- Update `DATABASES` in `settings.py` for your PostgreSQL credentials.
- Create Django apps as needed.

---
Generated by GitHub Copilot
